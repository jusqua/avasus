import { Header } from "@components/Header";

export function App() {
  return (
    <Header routes={[["Sobre Nós", "#"], ["Cursos", "#"], ["Parceiros", "#"], ["Transparência", "#"], ["Contato", "#"]]}/>
  );
}
